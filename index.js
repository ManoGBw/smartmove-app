import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent chama o AppRegistry.runApplication('main', { rootTag });
// e garante que tudo funcione corretamente.
registerRootComponent(App);